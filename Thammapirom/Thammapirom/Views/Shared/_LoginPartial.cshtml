@if (Request.IsAuthenticated)
{
    <text>
        <p id="greeting">สวัสดี, @Html.ActionLink(User.Identity.Name, "Manage", "Account", routeValues: null, htmlAttributes: new { @class = "username", title = "Manage" })
        @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
        {
            @Html.AntiForgeryToken()
            <a id="logout" href="javascript:document.getElementById('logoutForm').submit()">ออกจากระบบ</a>
        }
        <p>
    </text>
}
else
{

}
