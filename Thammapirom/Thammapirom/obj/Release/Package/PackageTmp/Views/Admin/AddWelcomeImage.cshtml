@model Thammapirom.Models.WelcomeImage

@{
    ViewBag.Title = "เพิ่มรูปภาพไปยังสไลด์";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
@if (Session["LoginStatus"] == "True")
{
    <h2>เพิ่มรูปภาพไปยังสไลด์</h2>
    using (Html.BeginForm("AddWelcomeImage", "Admin"))
    {
           @Html.ValidationSummary()
    <div>
        อัพโหลดภาพใหม่:
                @Html.EditorForModel()

    </div>
   
    <input type="submit" value="บันทึก" />
    @Html.ActionLink("Cancel and return to List", "IndexManaging")
    }
}
else { }