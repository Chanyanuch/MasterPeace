@model Thammapirom.Models.GalleryImage


@{
    ViewBag.Title = "เพิ่มรูปภาพหน้าแกลอรี่";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
@if (Session["LoginStatus"] == "True")
{
    <h2>เพิ่มรูปภาพหน้าแกลอรี่</h2>
    using (Html.BeginForm("AddGalleryImage", "Admin"))
    {
           @Html.ValidationSummary()
    <div>
        อัพโหลดภาพใหม่:
                @Html.EditorForModel()

    </div>
   
    <input type="submit" value="บันทึก" />
    @Html.ActionLink("ยกเลิกและกลับไปยังหน้าจัดการแกลอรี่", "GalleryManaging")
    }
}
else { }